const chai = require('chai')
const routes = require('../routes')

const expect = chai.expect

describe('api', () =>
    describe('GET /', () =>
        it('respond with an array of users', () => {

            expect("ee").to.be.a('string')
        })
    )
)